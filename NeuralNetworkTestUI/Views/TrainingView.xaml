<UserControl x:Class="NeuralNetworkTestUI.Views.TrainingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:NeuralNetworkTestUI.ViewModels"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" 
             d:DataContext="{d:DesignInstance Type=viewModels:CalculationViewModel, IsDesignTimeCreatable=True}"
             cal:Bind.AtDesignTime="True">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="30" Grid.RowSpan="2"/>
        <DataGrid HorizontalAlignment="Stretch" Grid.RowSpan="2" Grid.Row="1" CanUserAddRows ="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Test1" Binding="{Binding [0]}"/>
                <DataGridTextColumn Header="Test2" Binding="{Binding [1]}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
